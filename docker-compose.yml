version: '3'
services:
  frontend:
    build: ./
    container_name: my-angular-app
    ports:
      - '51008:80'
  database:
    image: mongo
    container_name: mongo-db-test
    ports:
      - '27017:27017'
  backend:
    build: ./server/
    container_name: express-server
    ports:
      - '3000:3000'
    links:
      - database